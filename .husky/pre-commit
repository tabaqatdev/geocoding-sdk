#!/usr/bin/env sh

# Run lint-staged for formatting and linting
bun run lint-staged

# Verify formatting on all source files (not just staged)
bun run format:check

# Run typecheck to ensure no type errors
bun run typecheck

# Verify build works and generates type definitions
bun run build:check
